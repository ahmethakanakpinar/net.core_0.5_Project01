@using EntityLayer.Concrete
@model List<WriterMessage>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_WriterLayout.cshtml";
}


<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Duyurular</h4>
            <div class="table-responsive">
                <table class="table">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Başlık</th>
                            <th>Tarih</th>
                            <th>Tür</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                          @*   var badgecolor = "";
                            if (item.Status == "Yeni Yazı")
                            {
                                badgecolor = "badge badge-danger";
                            }
                            else if (item.Status == "Yeni Video")
                            {
                                badgecolor = "badge badge-warning";
                            }
                            else if (item.Status == "Konferans")
                            {
                                badgecolor = "badge badge-info";
                            }
                            else if (item.Status == "WorkShop")
                            {
                                badgecolor = "badge badge-success";
                            } *@
                            <tr>
                                <td>deneme</td>
                                <td>deneme</td>
                                <td>@(((DateTime)item.Date).ToString("dd MMM yyyy"))</td>
                                <td><a href="#" class="btn btn-outline-primary" data-toggle="modal" data-target="#exampleModalCenter_@item.WriterMessageID">Detaylar</a></td>
                            </tr>

                            <!-- Modal -->
                            <div class="modal fade" id="exampleModalCenter_@item.WriterMessageID" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
                                <div class="modal-dialog modal-dialog-centered" role="document">
                                    <div class="modal-content">
                                        <div class="modal-header">
                                            <h5 class="modal-title" id="exampleModalLongTitle">Detaylar</h5>
                                        </div>
                                        <div class="modal-body">
                                            <span>deneme</span>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>


